import { defineAppSetup } from '@slidev/types'
import '../styles/base.css'
import '../styles/mermaid.css'

export default defineAppSetup(() => {
  // Components are auto-imported by Slidev from the components/ directory
  // Layouts are auto-imported by Slidev from the layouts/ directory
})
